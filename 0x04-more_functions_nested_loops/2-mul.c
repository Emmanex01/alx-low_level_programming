#include "main.h"

/**
 * mul - multiplies two integer
 *
 * @a: integer
 * @b: integer
 *
 * Return: always return 0
 */
int mul(int a, int b)
{
	if ((a <= '0' || a >= '0') && (b <= '0' || b >= '0'))
	{
		_putchar("a * b");
	}
	return (0);
}
