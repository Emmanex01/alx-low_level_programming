#include <stdio.h>

/* main - prints number of arguments
 * @argc: counts number of arguments
 * @argv: contains different arguments
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
