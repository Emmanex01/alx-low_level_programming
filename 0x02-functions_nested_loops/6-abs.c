#include "main.h"

/**
 * _abs - main entry
 *
 * @n: An integer
 *
 * Return: all returns positive interger
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else (n < 0)
	{
		return (n * -1);
	}
}
